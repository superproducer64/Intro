## 1. Dead / Abandoned Code

### `Intro/` directory
- **File/Directory:** `Intro/`
- **Description:** Full parallel TypeScript app tree (has its own `app.json`, `tsconfig.json`, `AGENTS.md`, `CLAUDE.md`, `components/`, `constants/`). Last touched 2026-06-02 via an automated "Published your App" commit (`b26063d`) — not hand-authored development. Not referenced by any `.replit` workflow (`.replit` defines only a "Web Server" task running `node server.js` and an "Expo Mobile" task running `cd mobile && npx expo start --tunnel`). No other file in the repo imports from it. Two doc-only mentions of "Intro/" were found, neither a code dependency: (1) `attached_assets/Docs/content-1774969059382.md`, a scraped GitHub page dump, repeatedly references `github.com/superproducer64/Intro/...` — this is the project's own GitHub repo name, unrelated to the local `Intro/` directory; (2) `.agents/memory/eas-ios-cloud-build.md:64` mentions `ios/Intro/Info.plist`, which refers to `mobile/ios/Intro/`, the native iOS project folder generated by `expo prebuild` and named after the app — also unrelated to this scaffold directory. No mention of the `Intro/` directory itself was found in `replit.md` or `README.md`.
- **Tag:** dead-code
- **Recommendation:** Remove. If anything inside (e.g. copy, constants) is still wanted, extract it explicitly into `mobile/` first — don't keep the directory around "just in case."

### `app/` directory
- **File/Directory:** `app/`
- **Description:** Single-file expo-router-style screen (`app/auth.tsx`) importing `../services/api` and `expo-router`. Last touched 2026-06-01 via the same automated "Published your App" commit pattern as `Intro/` (commit `c9d09a2`, which also added root `services/`). Root `package.json` lists only backend dependencies (`express`, `pg`, `ws`, `cors`, etc.) — `expo-router` is absent, so `app/auth.tsx`'s `import { router } from 'expo-router'` would never have resolved at the root level, confirming this was never a working app, just scaffold. Not referenced by any `.replit` workflow or any other file in the repo.
- **Tag:** dead-code
- **Recommendation:** Remove alongside `Intro/` and root `services/` — they form one abandoned scaffold.

### Root `services/` directory
- **File/Directory:** `services/api.js`
- **Description:** Only consumer found is `app/auth.tsx`. A repo-wide grep for relative imports of `services` (`require('./services'`, `require('../services'`, `from './services'`, `from '../services'`) against `server.js`, root `*.js`, `mobile/src`, and `public` returned two hits — `mobile/src/components/ReportBlockModal.js:4` and `mobile/src/navigation/AppNavigator.js:7` — but both import `'../services/api'` relative to their own location inside `mobile/src/`, which resolves to `mobile/src/services/api.js`, not this root file. No backend or mobile file actually requires root `services/api.js`. Distinct from `mobile/src/services/` (which has its own `api.js` and `supabase.js` and is actively used).
- **Tag:** dead-code
- **Recommendation:** Remove together with `app/`.

### Flutter/IDE leftovers (`.metadata`, `intro.iml`)
- **File/Directory:** `.metadata`, `intro.iml`
- **Description:** `.metadata` is Flutter-tool-generated ("This file tracks properties of this Flutter project... Used by Flutter tool to assess capabilities and perform upgrades etc.") and lists Flutter migration metadata for android/ios/linux/macos/web/windows platforms plus `lib/main.dart` as an unmanaged file — none of which exist in this repo. `intro.iml` is a JetBrains IDE module file declaring a `JAVA_MODULE` with `lib`/`test` source folders and a "Dart SDK" / "Flutter Plugins" / "Dart Packages" library — also Flutter/Dart tooling artifacts. Both predate the current Node+Expo architecture — `README.md` describes the original concept as "A Flutter app called Intro," but the live app is React Native/Expo (`mobile/`), not Flutter. Both files last touched 2025-10-24 (commit `644daeb`, "Add files via upload"), well before the current backend/mobile architecture's active development.
- **Tag:** dead-code
- **Recommendation:** Remove both; neither is read by any current tooling (Node backend, Expo config, or `.replit`).
